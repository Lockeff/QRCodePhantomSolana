{
    "version": 2,
    "builds": [
      {
        "src": "package.json",
        "use": "@vercel/static-build",
        "config": {
          "distDir": "web-build"
        }
      }
    ],
    "buildCommand": "npm install --legacy-peer-deps && npm run build"
  }
  